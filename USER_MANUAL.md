# 聖經神學筆記 使用手冊

**訪問網址：** http://192.168.50.8:3000

---

## 目錄

1. [基本概念](#基本概念)
2. [新增筆記](#新增筆記)
3. [讀經模式](#讀經模式)
4. [瀏覽與搜尋筆記](#瀏覽與搜尋筆記)
5. [經文快速查閱](#經文快速查閱)
6. [標籤管理](#標籤管理)
7. [系統神學教義連結](#系統神學教義連結)
8. [主題追蹤鏈](#主題追蹤鏈)
9. [交叉引用](#交叉引用)
10. [外部資料管理](#外部資料管理)
11. [匯出研究文件](#匯出研究文件)
12. [草稿保護與修訂歷史](#草稿保護與修訂歷史)
13. [AI 輔助分析](#ai-輔助分析)
14. [資料備份與還原](#資料備份與還原)

---

## 基本概念

本應用程式以**筆記**為核心單元，每則筆記對應一段聖經經文，可以附加：

- **聖經神學標籤**（Biblical Theology）：跨書卷的正典主題，如「約」、「彌賽亞」
- **系統神學標籤**（Systematic Theology）：教義分類，如「基督論」、「救恩論」
- **教義連結**：記錄此段經文如何貢獻於某個教義的具體說明
- **交叉引用**：連結其他相關筆記
- **外部資料**：連結網頁文章、書籍等參考資料

累積下來的筆記可以從三個方向重新組織：依**經文**（讀經模式）、依**教義**（教義頁）、依**主題發展脈絡**（追蹤鏈），並可整批匯出成研究文件。

---

## 新增筆記

1. 點擊左側欄「**新增筆記**」按鈕（或頂部 **+** 按鈕）
2. 選擇**書卷**、**章節**（可選起迄範圍）、**節數**（可選）
3. 輸入**標題**（可選）與**筆記內容**（支援 Markdown 格式）
4. 右上角可選擇**套用範本**快速開始
5. 選擇相關的聖經神學與系統神學**標籤**
6. 加入**教義連結**（選擇教義 + 說明此段經文的貢獻）
7. 點擊「**儲存**」

> 從讀經模式進入新增筆記時，書卷、章、節會自動帶入，不需重選。

### Markdown 格式支援

```
# 大標題
## 小標題
**粗體**  *斜體*
- 列表項目
> 引用文字
`行內程式碼`
```

### 鍵盤快捷鍵

按 **⌘S**（Mac）或 **Ctrl+S**（Windows）可直接儲存，不需移動滑鼠到按鈕。

編輯中若嘗試離開頁面而尚未儲存，系統會提示確認。

---

## 讀經模式

一般的筆記系統是「先想到筆記，再找經文」。讀經模式是反過來的：**打開一卷書逐章讀，過去寫過的筆記會自動出現在對應的經節旁邊**。

### 開啟方式

左側欄點擊「**讀經模式 Reading**」。

### 操作方式

| 功能 | 說明 |
|------|------|
| 書卷下拉選單 | 選擇要閱讀的書卷 |
| 上一章 / 下一章 | 在同一卷書內翻章 |
| 章節下拉選單 | 直接跳到指定章 |
| 本章新增筆記 | 建立一則涵蓋整章的筆記 |
| 每節右側 **+** | 針對**該節**新增筆記，經文範圍自動帶入 |

### 已有筆記的經節

- 該節會以較明顯的樣式標示
- 節文下方出現筆記標題的小標籤，**點擊即可跳到該則筆記**
- 一節若對應多則筆記，會全部列出

頁面頂端顯示「**本章 N 節已有筆記 · 共 M 節**」，可快速看出這一章的研讀密度。

> 涵蓋範圍會自動計算：整章筆記、單節筆記、跨章筆記都會正確對應到所屬的每一節。

---

## 瀏覽與搜尋筆記

### 筆記列表

左側欄點擊「**筆記 Notes**」，可依以下方式篩選：
- 書卷
- 聖經神學標籤
- 系統神學標籤

### 全域搜尋（⌘K）

按 **⌘K**（Mac）或 **Ctrl+K**（Windows）開啟全域搜尋。輸入 **2 個字以上**即開始搜尋。

搜尋範圍涵蓋四類內容，結果會分類顯示：

| 分類 | 搜尋的內容 |
|------|-----------|
| 筆記 | 標題、內文、經文範圍（含書卷中英文名） |
| 教義註解 | 教義連結裡填寫的說明文字 |
| 追蹤鏈 | 名稱與說明 |
| 資料 | 外部資料的標題、作者、出處、摘要、網址 |

搜尋結果會顯示**命中處的上下文片段**，關鍵字以高亮標示，讓你不必逐一點開就能判斷哪一筆才是要找的。

- **↑ ↓** 移動選取
- **Enter** 開啟
- **Esc** 關閉

點擊教義註解的搜尋結果，會直接跳到該註解所屬的筆記。

> 搜尋在伺服器端以全文索引進行，筆記量增加也不會變慢。新增或修改內容後索引會自動更新。

### Dashboard 總覽

首頁顯示：
- 筆記數、標籤數、追蹤鏈數統計
- 聖經進度圖（哪些書卷已有筆記）
- 標籤使用頻率分析

---

## 經文快速查閱

新增或查看筆記時，頁面會自動顯示「**經文參考**」面板，載入該段的中英對照原文（中文：和合本；英文：WEB 譯本）。

### 顯示模式

面板頂部有切換按鈕：

| 按鈕 | 說明 |
|------|------|
| 中文 | 只顯示和合本 |
| 中英 | 中英對照（預設） |
| English | 只顯示 WEB 英文 |
| 原文 | 舊約顯示希伯來文（WLC），新約顯示希臘文（Tischendorf） |

### 字體大小

點擊 **A−** / **A+** 調整經文字級，設定會記住，下次開啟仍維持。

### 節數範圍

- 若筆記指定了**節數範圍**（如 1:1–18），面板預設只顯示選定的節，並以金色高亮標示
- 面板底部有「**顯示整章**」按鈕，可切換查看完整章節
- 點選「**只顯示選擇節數**」可切換回來

### 收合面板

點擊面板右上角的 **∧** 按鈕可收合面板節省空間，再點 **∨** 展開。

> 經文資料內建於程式中，離線也可查閱，不需連網下載。

---

## 標籤管理

左側欄點擊「**標籤管理 Tags**」。

### 聖經神學標籤（Biblical）

代表跨書卷的正典主題，預設提供：
約、國度、聖殿、創造與新創造、流放與歸回、律法、獻祭、應許、子民、彌賽亞

### 系統神學標籤（Systematic）

代表神學教義分類，預設提供：
神論、基督論、聖靈論、人論、罪論、救恩論、教會論、末世論、聖經論、贖罪

### 新增 / 編輯 / 刪除標籤

- 點擊「**+ 新增標籤**」輸入名稱並選擇顏色
- 點擊現有標籤可編輯名稱或顏色
- 刪除標籤時，所有使用該標籤的筆記會自動移除該標籤

---

## 系統神學教義連結

在筆記編輯頁底部的「**系統神學教義連結**」區塊：

1. 點擊「**+ 連結系統神學教義**」
2. 從下拉選單選擇教義分類
3. 填寫**註解**：說明此段經文如何具體貢獻於此教義
   - 例：「逾越節羔羊的替代性死亡，預表基督代贖的核心邏輯」
4. 點擊「**新增**」

在「**教義 Doctrines**」頁面可以從教義的角度，查看所有連結到某個教義的經文與筆記，並可整批匯出成研究文件（見[匯出研究文件](#匯出研究文件)）。

> 這裡填寫的註解會被全域搜尋（⌘K）一併檢索。

---

## 主題追蹤鏈

追蹤一個神學主題從舊約到新約的發展脈絡。

### 建立追蹤鏈

1. 點擊左側欄「**追蹤鏈 Chains**」
2. 點擊「**+ 新增追蹤鏈**」
3. 輸入名稱、說明，選擇對應的聖經神學標籤
4. 加入相關筆記（按正典順序排列）

### 使用方式

追蹤鏈會以時間軸方式呈現各筆記，幫助觀察主題在救贖歷史中的發展。整條鏈可一鍵匯出成研究文件（見[匯出研究文件](#匯出研究文件)）。

---

## 交叉引用

在筆記詳情頁的「**交叉引用**」區塊：

1. 點擊「**+ 引用筆記**」
2. 搜尋並選擇目標筆記
3. 填寫引用說明（可選）
   - 例：「此處的羔羊意象與出埃及記 12 章互相呼應」
4. 點擊「**建立引用**」

引用分為「→ 引用」與「← 被引用」兩個方向，點擊可直接跳轉到被引用的筆記。

---

## 外部資料管理

### 新增資料來源

左側欄「**外部資料 Resources**」頁面，支援三種類型：

| 類型 | 適用場景 |
|------|---------|
| 🌐 網址 URL | 網路文章、講道錄音、線上工具 |
| 📚 書籍 Book | 神學書籍、注釋書 |
| 📄 其他 Other | 論文、私人筆記 |

### 連結到筆記

在筆記詳情頁的「**外部資料**」區塊，點擊「**+ 連結資料**」，從已建立的資料庫中搜尋並連結。

---

## 匯出研究文件

把累積的研究整批編譯成可交付的文件——備講道、寫查經材料、或與人討論神學時使用。

### 可匯出的兩種範圍

| 範圍 | 位置 |
|------|------|
| **一條追蹤鏈** | 追蹤鏈頁面，選定某條鏈後點「匯出研究」 |
| **一個教義** | 教義頁面，選定某個教義後點「匯出研究」 |

### 三種格式

| 格式 | 適用場景 |
|------|---------|
| **Markdown** | 貼進其他筆記軟體、版本控管、再加工 |
| **Word** | 需要進一步排版、加註、與人協作 |
| **PDF** | 直接列印或分發，版面固定 |

### 文件內容

匯出的文件會**依正典順序**排列所有相關筆記，每則包含：

1. 經文出處與筆記標題
2. **經文全文**（和合本 + WEB 英文對照）
3. 筆記內容
4. 該筆記的教義註解
5. 該筆記連結的參考資料清單（作者、出處、頁碼、網址）

> PDF 需要伺服器具備中文字型才能正確輸出。Docker 映像已內建 Noto CJK 字型；若在其他環境自行部署而 PDF 匯出報錯，可用 `PDF_FONT_PATH` 環境變數指定字型檔路徑。

---

## 草稿保護與修訂歷史

神學筆記通常是長文，這兩個機制確保寫過的內容不會消失。

### 自動草稿

編輯筆記時，內容會**每隔約 2 秒自動存成草稿**，存於瀏覽器本機。

- 編輯中頁面上方顯示「**草稿已自動儲存 HH:MM:SS**」
- 若瀏覽器分頁意外關閉、當機或斷線，下次開啟同一則筆記時會出現提示：
  **「找到自動儲存草稿」** + 草稿時間
  - 點「**還原草稿**」把未存檔的內容救回來
  - 點「**捨棄**」丟棄草稿，使用已儲存的版本
- 正常按下「儲存」後，草稿會自動清除

> 新增筆記（尚未儲存）時寫到一半離開，草稿同樣會被保留。

### 修訂歷史

每次儲存**有變動**的筆記時，系統會自動保留一份修改前的快照。

在筆記詳情頁的「**修訂歷史**」區塊：

1. 點擊「**查看歷史**」列出所有舊版本（時間、標題、字數）
2. 點「**預覽**」查看該版本的完整內容
3. 點「**還原**」把筆記回復到該版本

> **還原不會遺失現況**：執行還原前，系統會先把目前的內容也存成一個版本，因此隨時可以再還原回來。
>
> 內容完全沒有變動的儲存不會產生重複版本。所有版本都會包含在匯出備份中。

「三年前我怎麼理解這段經文」本身就是有價值的紀錄——修訂歷史讓神學理解的演變過程被保存下來。

---

## AI 輔助分析

在筆記詳情頁底部的「**AI 輔助分析**」區塊提供三種功能：

| 按鈕 | 功能 |
|------|------|
| 建議相關經文 | 根據筆記內容，建議主題相近的其他聖經段落 |
| 建議教義連結 | 分析筆記可連結的系統神學教義，並說明具體貢獻 |
| 延伸研究方向 | 建議下一步可深入探索的研究方向與問題 |

AI 分析結果會考慮你已有的標籤分類，讓建議能對應到系統中的教義架構。

### 建議可以直接寫入知識庫

AI 的每一條建議下方都有「**採納**」與「**忽略**」兩個按鈕：

| 建議類型 | 點「採納」的效果 |
|---------|----------------|
| 建議教義連結 | **直接建立教義連結**，把 AI 寫的註解存入該筆記 |
| 建議相關經文 | 若該段經文已有筆記，**直接建立交叉引用** |
| 延伸研究方向 | 標記為已採納（僅作為閱讀紀錄） |

- 建議相關經文時，若該段落**已有你的筆記**，旁邊會出現「**已有筆記**」按鈕可直接跳過去查看
- 若該段落還沒有筆記，「採納」會停用並提示先去讀經模式建立筆記
- 已處理的建議會標示「**已採納**」或「**已忽略**」

### 分析結果會被保存

分析結果存在資料庫中，**離開頁面再回來仍在**，不需重跑、也不會重複消耗 API 費用。

- 已有結果的功能按鈕旁會出現小圓點標記
- 點該按鈕直接顯示上次的結果
- 需要重跑時，點結果區的「**重新分析**」
- 即使之後移除了 API Key，已保存的分析仍可查看
- 分析結果會包含在匯出備份中

---

### 支援的 AI 供應商

支援四家供應商，擇一使用：

| 供應商 | API Key 取得網址 | Key 格式 |
|--------|-----------------|----------|
| **Anthropic** | console.anthropic.com | `sk-ant-...` |
| **OpenAI** | platform.openai.com | `sk-...` |
| **Google** | aistudio.google.com | `AIzaSy...` |
| **OpenRouter** | openrouter.ai/keys | `sk-or-v1-...` |

> **OpenRouter** 是一個聚合平台：用**一把 Key**就能呼叫上百種不同廠牌的模型（Anthropic、OpenAI、Google、DeepSeek、Llama 等），並可隨時切換比較。適合想試不同模型、或想集中管理帳單的情況。

### 設定 API Key（介面設定，建議）

1. 打開任一則筆記，捲到底部的「**AI 輔助分析**」
2. 點右上角的「**API Key**」
3. 在彈出的「**AI 服務設定**」視窗上方選擇供應商分頁
4. 貼上 API Key，點「**儲存**」

儲存後會自動切換為使用該供應商。Key 儲存在伺服器的資料目錄中，不會上傳到任何外部服務。

**切換供應商**：在該供應商的分頁點「**切換為使用此服務**」。已存有 Key 的分頁名稱旁會顯示小圓點，目前使用中的會標示「✓ 目前使用中」。

**移除 Key**：切到該分頁點「**移除 Key**」。若移除的是使用中的供應商，系統會自動改用其他還有 Key 的供應商。

### OpenRouter：選擇模型

選擇 OpenRouter 分頁時，會多出一個「**使用的模型**」欄位：

- 點擊欄位會出現**完整模型清單**（從 OpenRouter 即時取得，數百種可選）
- 也可以直接輸入模型代號，例如 `anthropic/claude-sonnet-4.5`、`google/gemini-2.5-flash`、`deepseek/deepseek-chat`
- **留空**則使用預設模型 `anthropic/claude-sonnet-4.5`
- 修改後離開欄位即自動儲存

設定完成後，AI 輔助分析面板的標題旁會顯示目前使用的供應商與模型，例如「**OpenRouter · gemini-2.5-flash**」。

> 若模型清單載入失敗（例如暫時連不上 OpenRouter），欄位旁會出現「模型清單載入失敗，重試」，此時仍可手動輸入模型代號。

### 用環境變數設定（替代方式）

也可以在啟動容器時用環境變數提供 Key。環境變數的優先權**高於**介面設定，且介面上會顯示為唯讀。

| 環境變數 | 用途 |
|---------|------|
| `ANTHROPIC_API_KEY` | Anthropic 金鑰 |
| `OPENAI_API_KEY` | OpenAI 金鑰 |
| `GOOGLE_API_KEY` | Google 金鑰 |
| `OPENROUTER_API_KEY` | OpenRouter 金鑰 |
| `OPENROUTER_MODEL` | OpenRouter 使用的模型（可省略） |

SSH 進 NAS，執行：

```bash
sudo docker rm -f bible-notebook && \
sudo docker run -d \
  --name bible-notebook \
  -p 3000:3000 \
  -v /volume1/docker/bible-notebook/data:/app/backend/data \
  -e OPENROUTER_API_KEY=sk-or-v1-你的金鑰 \
  -e OPENROUTER_MODEL=anthropic/claude-sonnet-4.5 \
  --restart unless-stopped \
  bible-theology-notebook
```

### 停用 AI 功能

不傳入任何 API Key 重新啟動容器，並在介面上移除已儲存的 Key 即可：

```bash
sudo docker rm -f bible-notebook && \
sudo docker run -d \
  --name bible-notebook \
  -p 3000:3000 \
  -v /volume1/docker/bible-notebook/data:/app/backend/data \
  --restart unless-stopped \
  bible-theology-notebook
```

停用後，AI 輔助面板會顯示提示，其他所有功能不受影響，**過去已保存的分析結果仍可查看**。

### 確認 AI 功能狀態

瀏覽器開啟：http://192.168.50.8:3000/api/ai/status

- `{"hasKey":true,"provider":"openrouter","model":"anthropic/claude-sonnet-4.5"}` → AI 已啟用，顯示目前的供應商與模型
- `{"hasKey":false,"provider":null,"model":null}` → AI 未啟用

---

## 資料備份與還原

### 匯出備份

左側欄底部點擊「**匯出備份**」，下載 JSON 格式備份檔，包含所有筆記、標籤、追蹤鏈、教義連結、交叉引用、外部資料、**筆記修訂歷史**與 **AI 分析結果**。

### 匯入還原

點擊「**匯入還原**」，選擇備份的 JSON 檔案，確認後覆蓋現有資料。

> **注意**：匯入會覆蓋全部現有資料，請先匯出備份。

### 直接備份資料庫檔案

更完整的備份方式是直接複製 SQLite 資料庫：

```bash
cp /volume1/docker/bible-notebook/data/notebook.db \
   /volume1/docker/bible-notebook/data/notebook.db.backup
```

> 資料目錄中的 `settings.json` 存有 API Key，若要完整搬遷請一併複製；若備份要外傳給他人，請**排除**這個檔案。
